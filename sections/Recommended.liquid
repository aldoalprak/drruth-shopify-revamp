<div class="premiumproduct">
 <div class="recommhead">
    <h2 class="recomtitle">{{section.settings.reccheaddd}}</h2>
 </div>
  {% for block in section.blocks %}
   {%- assign product = block.settings.product -%}
    <div class="premiumproductflex">
        <div class="imagewrapper">
            <div class="desktopimg">
                <img src="{{ product.featured_image | img_url: 'medium' }}">
            </div>
            <div class="desktoptitle">     
              <h2 class="premiumprtitle">{{ product.title | escape }}</h2>
              <h2 class="preeprice" >{% render 'price', product: product, price_class: '' %}</h2>
             </div>
        </div>
        <div class="queandbtn">
            <div class="quecontent">
                    <h2 class="proquee">{{block.settings.proquee}}</h2>
                    <p class="proanss">{{block.settings.proanss}}</h2>
            </div>
            <div class="product-form__buttons">
                    {%- liquid
                        assign check_against_inventory = true
                        if product.selected_or_first_available_variant.inventory_management != 'shopify' or  product.selected_or_first_available_variant.inventory_policy == 'continue'
                        assign check_against_inventory = false
                        endif
                        if product.selected_or_first_available_variant.quantity_rule.min > product.selected_or_first_available_variant.inventory_quantity and check_against_inventory
                        assign quantity_rule_soldout = true
                        endif
                    -%}
                    <button
                        id = "ProductSubmitButton-{{ section_id }}"
                        type="submit"
                        name="add"
                        class="reccommbtnn product-form__submit button button--full-width {% if block.settings.show_dynamic_checkout %}button--secondary{% else %}button--primary{% endif %}"
                        {% if product.selected_or_first_available_variant.available == false or quantity_rule_soldout %}
                        disabled
                        {% endif %}
                    >
                        <span>
                        {%- if product.selected_or_first_available_variant.available == false or quantity_rule_soldout -%}
                            {{ 'products.product.sold_out' | t }}
                        {%- else -%}
                            {{ 'products.product.add_to_cart' | t }}
                        {%- endif -%}
                        </span>
                        <div class="loading-overlay__spinner hidden">
                        <svg
                            aria-hidden="true"
                            focusable="false"
                            class="spinner"
                            viewBox="0 0 66 66"
                            xmlns="http://www.w3.org/2000/svg"
                        >
                            <circle class="path" fill="none" stroke-width="6" cx="33" cy="33" r="30"></circle>
                        </svg>
                        </div>
                    </button>
                    {%- if block.settings.show_dynamic_checkout -%}
                        {{ form | payment_button }}
                    {%- endif -%}
                </div>
        </div>
    </div>
     {% endfor %}
</div>

{% schema %}
{
"name": "Recommended",

"settings": [
  {
          "type": "text",
          "id": "reccheaddd",
          "label": "Title"
        }
        
],
           "blocks": [
  {
    "type": "collection",
    "name": "Collection",
    "settings": [
        {
          "type": "product",
          "id": "product",
          "label": " Product Image"
        },
    {
        "type":"text",
        "id":"proquee",
        "label":"Questions"
    },
    {
        "type":"text",
        "id":"proanss",
        "label":"Answer"
    }
    ]
  }

],

"presets" : [
{
  "name" : "Recommended",
  "category" : "premiumproduct"

  }
]
}
{% endschema %}

 {% stylesheet %}
/*   .premiumconten {
    text-align: center;
    display: flex;
    justify-content: center;
    align-items: center;
} 
    .premiumproduct {
      background-color: #F7F3ED;
      position: relative;
      margin-top:80px;
      padding: 0 200px;
    }
    .premiumproductflex{
      display:flex;
      flex-wrap:wrap;
      align-items:center;
    }
    .mobileshowtitle {
      display: none;
    }
    .imagewrapper{
      flex: 0 0 50%;
      max-width: 50%;
    }
    .premiumconten{
      flex: 0 0 50%;
      max-width: 50%;
    }
    .mbimg{
      display:none;
    }
    .premiumconten{
      text-align: center;
    }

   .ratingstar {
      display: flex;
      flex-wrap: wrap;
     justify-content: center;
    }
  
    .premiumsub{
      font-style: normal;
      font-weight: 400;
      font-size: 38px;
      line-height: 30px;
      color: #95694C;
      margin-bottom: 60px;
    }
    .premiumbtn {
      background: #95694C;
      border: 1px solid #FFFFFF;
      border-radius: 6px;
      font-style: normal;
      font-weight: 600;
      font-size: 22px;
      line-height: 15px;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #fff;
      padding: 20px 35px;
  }
  @media only screen and
  (max-width: 1305px){
    .premiumproduct {
      padding: 0px 30px;
    }
  }
  @media only screen and
  (max-width: 991px){
    
    .premiumsub{
      font-size: 25px;
    }
  }

  @media only screen and
  (max-width: 865px){
    .premiumsub{
      font-size: 26px;
    }
    
  }
  @media only screen and
  (max-width: 768px){
    .premiumproduct {
      padding: 20px 0;
    }
    .premiumproductflex {
      display: block;
    }
    .imagewrapper, .premiumconten {
      max-width: 100%;
    }
    .mobileshowtitle {
      text-align: center;
    }
    .desktopimg{
          background-color: transparent;
    }
    .mbimg {
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .mbimg img{
      max-width:50%;
    }
    .desktoptitle {
      display: none;
    }
    .premiumbtnmain{
      display: flex;
      align-items: center;
      justify-content: center;
      align-items: center;
    }
    .mobileshowtitle {
      display: block;
    }
  } */
{% endstylesheet %} 

{% javascript %}
{% endjavascript %}

